import { getRules, createRule, destroyRule, updateRule, patchRule } from './rule'
import { getRecords } from './record'

export default {
  getRules,
  createRule,
  destroyRule,
  updateRule,
  patchRule,

  getRecords,
}